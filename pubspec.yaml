name: music_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^10.2.0
  device_info_plus: ^9.0.2
  path_provider: ^2.0.15
  just_audio: ^0.9.34
  audio_video_progress_bar: ^1.0.1
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "android/assets/img/logo.jpg"


flutter:
  uses-material-design: true